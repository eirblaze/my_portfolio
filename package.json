{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "my portfolio",
  "main": "index.js",
  "repository": "git@github.com:eirblaze/my_portfolio.git",
  "author": "eirblaze <eirblaze@live.jp>",
  "license": "no license",
  "private": true,
  "scripts": {
    "ts": "npm run --prefix ./functions build",
    "sv": "npm run --prefix ./functions build && firebase serve",
    "up": "firebase deploy",
    "start": "yarn --cwd './front' start",
    "js": "yarn --cwd './front' js",
    "js-r": "yarn --cwd './front' js-r",
    "lint": "eslint ./**/*.ts ./**/*.js ./**/*.mjs"
  }
}
